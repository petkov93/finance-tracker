# System appearance excludes Cool Ledger

Theme preference **System** continues to resolve only to **Warm Ledger** or **Night Ledger** from the OS color scheme. **Cool Ledger** is an explicit stored preference and is never auto-selected by System, so OS light/dark stays a simple Warm/Night pair without inventing a Cool dark twin or dual light/dark prefs.
